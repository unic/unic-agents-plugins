# Plan — spec-08: Version bump, README, CLAUDE.md

1. Step 1 — Update docs and bump version
   - Demo: `plugin.json` shows `0.8.0`, `verify:changelog` passes, CLAUDE.md and README.md updated, `pnpm -w check` passes, spec marked done, committed
   - Wave: single atomic task covering all file edits + bump + commit
