# Step 08 — `/qa`

> **Read [PLAN.md](./PLAN.md) + [README.md](./README.md).** Largely reusable; add the on-ramp role.

## Goal
Refine the existing `qa` workflow: e2e → coverage-gate → UAT (HITL) → merge. Additionally recognise `/qa` as an **issue-producing on-ramp** (PLAN #5): findings during QA become agent-ready issues feeding `/build`.

## Task
- Keep `e2e → coverage-gate → uat-gate → verify-pr-base → merge`. Update gate expression to the confirmed schema; honour `gates.qa` (UAT + merge HITL by default).
- **Add finding-capture:** when UAT surfaces a defect, file it as a tracker issue via `lib/tracker-adapter.mjs` (reuse `/triage`'s brief format or Matt's `qa` skill conversational filing) rather than only halting.
- Update artifact paths to `workflows/<slug>/` (contract C).

## Open questions to grill first
- How QA findings become issues — inline here, or hand to `/triage`? (Prefer: file directly with a brief, mark blocked relationships honestly — like Matt's `/qa`.)
- Coverage-gate default threshold (currently null/skip) — keep configurable.

## Done when
`/qa <slug>` runs e2e/coverage/UAT/merge with HITL gates, and a UAT finding produces an agent-ready issue. PR to `develop`.

## Suggested skills
`/archon`, `/grilling`. Reference `.agents/skills/qa/SKILL.md`.
