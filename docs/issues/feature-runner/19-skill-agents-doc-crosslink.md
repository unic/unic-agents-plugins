# Add cross-link from SKILL.md to `docs/agents/feature-runner.md`

**Status:** closed
**Category:** documentation

> _This was generated by AI during triage._

## Parent

`docs/issues/feature-runner/PRD.md`

## What to build

`SKILL.md` and `docs/agents/feature-runner.md` serve distinct audiences and should remain separate:

- `SKILL.md` — runtime guidance for the orchestrating Claude instance.
- `docs/agents/feature-runner.md` — human-facing reference: lifecycle diagram, dependency-satisfaction matrix, ADR scope, historical cleanup convention.

Currently there is no link from SKILL.md to the agents doc. A human reading the skill (or a developer extending it) has no signal that a fuller reference exists.

### Change

In `SKILL.md` Supporting Documentation section, add one line pointing to the agents doc:

```markdown
- **`docs/agents/feature-runner.md`** — human-facing reference: lifecycle diagram, dependency-satisfaction matrix, ADR scope, historical cleanup convention. Maintained in parallel; consult for broader context, not for runtime instructions.
```

No prose is removed from either file.

## Acceptance criteria

- [ ] `SKILL.md` Supporting Documentation section includes a pointer to `docs/agents/feature-runner.md` with a one-line description
- [ ] No prose is removed from SKILL.md or `docs/agents/feature-runner.md`
- [ ] SKILL.md word count remains under 2,000 words after the addition

## Blocked by

None — can start immediately.
