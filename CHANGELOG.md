# Changelog

Notable changes to the `unic-agents-plugins` monorepo workspace and shared packages.

For plugin-specific changelogs see each plugin's `CHANGELOG.md` under `apps/claude-code/<name>/`.

## [Unreleased]

### Added

- Initial monorepo seed
